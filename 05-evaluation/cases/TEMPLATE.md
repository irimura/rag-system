# TCxx: (観点名) — ケース手順書

> このテンプレートを複製して TC02〜TC06 / TC08 / TC10 の手順書を作成する。
> 観点の定義は [evaluation-spec.md](../../docs/evaluation-spec.md) §4 を参照。

| 項目 | 内容 |
|---|---|
| 観点 ID | TCxx |
| 観点 | (何を検証するか 1 行で) |
| 主な検証対象 | (Retriever / Reranker / 生成 / クエリ書き換え 等) |
| 使用ケース | ゴールデンデータセットの `TCxx-*`(最低 5 件) |

## 1. 目的

(この観点で検出したい失敗モードを具体的に書く。例: 「〜のとき、システムが〜してしまう」)

## 2. 前提条件

- [ ] 対象の案が構築済みで、評価用コーパスの取り込みが完了している
- [ ] ゴールデンデータセットに本観点のケースが登録されている
- [ ] (観点固有の前提があれば追記)

## 3. データ準備

- 質問の作り方: (この観点の質問を追加する際の指針。何を変え、何を固定するか)
- evidence の指定: (採点用の quote は必須。監査用の doc_id をどう選ぶか)

## 4. 実行手順

### 4.1 自動評価(レベル1/レベル2 のカテゴリ別スコア)

```bash
cd test/level1 && python run_level1.py            # カテゴリ別 HitRate を確認
cd ../level2 && python run_level2.py              # カテゴリ別 生成品質を確認
```

### 4.2 個別ケースの手動確認

```bash
cd test/level1 && python run_level1.py --case TCxx-001 --verbose
```

1. WebUI(Open WebUI / Chainlit)を開く
2. ケースの質問を**そのまま**入力する
3. 回答と参考資料の表示を記録する(スクリーンショット推奨)

## 5. 期待結果と判定基準

| # | 確認項目 | 期待結果 | 判定 |
|---|---|---|---|
| 1 | (検索面の確認項目) | (期待) | 合/否 |
| 2 | (回答面の確認項目) | (期待) | 合/否 |
| 3 | 出典表示 | 根拠となった文書名が参考資料に含まれる | 合/否 |

- 迷ったら「否」に倒す

## 6. 不合格時の切り分け

1. `--verbose` の取得チャンクを確認 → 正解根拠が候補に入っているか(入っていない = Retriever、入っているが上位でない = Reranker)
2. (観点固有の切り分け手順)

## 7. 記録

- 実施日 / 実施者 / 構成(実験 ID) / 各ケースの判定 / 気づき を `eval/experiments.md` または課題管理に記録する
