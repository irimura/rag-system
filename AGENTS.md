# AGENTS.md — コーディングエージェント向けガイド

このリポジトリで作業を始める前に、**必ず [agent/session.md](agent/session.md) を読むこと。**

過去セッションの前提・設計判断・執筆規約・Git 運用・既知の罠・未完了事項のすべてがそこに集約されている。記載された決定(ノード命名、Ubuntu 24.04、EICE 採用、一時 NAT+IGW、固定プライベート IP 等)を、ユーザー確認なしに覆さないこと。

## agent/ ディレクトリの運用ルール

- `agent/` 配下で恒久的に維持するのは `session.md` のみとする。引き継ぐべき決定・要点・未確認事項はすべて `session.md` に集約する。
- レビュー記録・エージェント間の依頼プロンプト・作業メモ等の一時ファイルは、役割(レビューサイクルや依頼の実行)が終了した時点で、要点を `session.md` へ集約したうえで同じ作業単位内で削除する。次のエージェントへ残さない。
- 一時ファイルを作成する場合はコミットに含めてよいが、削除までを作成した側のワークフローの一部として扱う。削除後の詳細参照は git 履歴を正とする。
- 例外として、一時ファイルを残したまま停止する場合(多セッションに跨るレビューサイクルの途中など)は、`session.md` に「何が残っていて、いつ消すか」を明記する。
