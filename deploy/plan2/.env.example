# === Node A(GPU ノード)の vLLM ===
VLLM_BASE_URL=http://node-a.example.internal:8080/v1
# Node A の SERVED_MODEL_NAME と一致させる(deploy/node-a/.env.example の SERVED_MODEL_NAME)
VLLM_MODEL=your-hf-model-name
# Node A の --api-key と同じ値にする
VLLM_API_KEY=change-me

# === Open WebUI に表示するモデル名(rag-api が公開する仮想モデル)===
RAG_MODEL_NAME=knowledge-rag

# === Embedding / Rerank(TEI コンテナ・CPU)===
# bge-m3 はプレフィックス不要。e5 系に変える場合は rag-api 側の前処理が必要
EMBED_MODEL=BAAI/bge-m3
RERANK_MODEL=BAAI/bge-reranker-v2-m3

# === 接続先(コンテナ内。通常変更不要)===
QDRANT_URL=http://qdrant:6333
QDRANT_COLLECTION=knowledge
TEI_EMBED_URL=http://tei-embed:80
TEI_RERANK_URL=http://tei-rerank:80

# === RAG パラメータ ===
RETRIEVE_K=20
RERANK_TOP_N=4
RERANK_THRESHOLD=0.1
CHUNK_SIZE=500
CHUNK_OVERLAP=100
DOCS_DIR=/data/documents

# === セキュリティ ===
# 各値を別々に生成する: openssl rand -hex 32
WEBUI_SECRET_KEY=change-me
FORWARD_USER_INFO_HEADER_JWT_SECRET=change-me
EVAL_TOKEN=change-me
ENABLE_LOGIN_FORM=true

# === OIDC(任意。主例: 外部 IdP)===
# OAuth 系は PersistentConfig のため初回起動前に設定するか Admin UI で更新する
# ENABLE_OAUTH_SIGNUP=true
# OAUTH_CLIENT_ID=replace-with-issued-client-id
# OAUTH_CLIENT_SECRET=replace-with-issued-client-secret
# OPENID_PROVIDER_URL=https://idp.example.com/.well-known/openid-configuration
# OAUTH_PROVIDER_NAME=Corporate-IdP
# OAUTH_MERGE_ACCOUNTS_BY_EMAIL=true
# ENABLE_OAUTH_GROUP_MANAGEMENT=true
# ENABLE_OAUTH_GROUP_CREATION=true
# OAUTH_GROUP_CLAIM=groups

# 外部 IdP の開通・経路整備前に Keycloak で先行検証する場合は上記4項目を置き換える
# OAUTH_CLIENT_ID=open-webui
# OAUTH_CLIENT_SECRET=open-webui-local-verification-secret
# OPENID_PROVIDER_URL=http://keycloak:8080/realms/rag/.well-known/openid-configuration
# OAUTH_PROVIDER_NAME=Keycloak

# === 検証用 Keycloak(profile: idp)===
KEYCLOAK_ADMIN_USERNAME=admin
# 生成例: openssl rand -hex 32
KEYCLOAK_ADMIN_PASSWORD=change-me
